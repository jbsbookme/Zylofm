# ZyloFM Backend API (NestJS)
# Copy to .env and fill values

# Server
PORT=3000
NODE_ENV=development

# Security
# Must be at least 16 chars.
JWT_SECRET=replace-me-with-a-long-random-string

# Database (Postgres)
DATABASE_URL=postgresql://user:password@localhost:5432/zylofm

# Seed admin (used by `npm run seed`)
ADMIN_EMAIL=admin@zylo.fm
ADMIN_PASSWORD=change-this-password

# If true, the API will upsert the admin on startup (recommended for first prod deploy)
AUTO_SEED_ADMIN=true

# Cloudinary (uploads)
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=

# Upload rules
# In local/dev this defaults to allowed; in production you should set explicitly.
ALLOW_ADMIN_UPLOAD=true

# CORS (comma-separated origins for browsers)
# Example: https://admin.yourdomain.com
CORS_ORIGINS=
